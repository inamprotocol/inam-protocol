{
  "name": "inam-protocol-docs-site",
  "version": "0.1.0",
  "private": true,
  "description": "docs.inamprotocol.org — static site: landing page, rendered SPEC.md, and a Redoc-powered openapi.yaml reference",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && npx wrangler dev",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "dependencies": {
    "marked": "^14.1.2",
    "redoc": "^2.2.0"
  },
  "devDependencies": {
    "wrangler": "^4.127.0"
  }
}
