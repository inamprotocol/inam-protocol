# Status / Where We Left Off

Last updated: 2026-09-17. This file exists so a new session (human or Claude) can pick up exactly where the last one stopped, without re-deriving it from chat history. It is a full audit, not just a pointer — read it before trusting any specific claim elsewhere, and re-verify anything time-sensitive (published package versions, live deployment state) rather than assuming this file is still current forever.

## Phase overview

| Phase | Status | Notes |
|---|---|---|
| 1 — Hardening | **Done** | D1 race fixes, rate limiting, CORS policy, FK enforcement, real Worker test suite |
| 2 — Public protocol | **Done** | GitHub org/repo, docs site, OpenAPI, versioning discipline |
| 3 — Real cross-agent job workflow | **Done** | Job resource (SPEC.md §3), all three runtimes |
| 4 — Verified external identity | **Done** | Link challenges (SPEC.md §2.1), ATTP-aligned, all three runtimes |
| Verification v0.1 (added mid-stream, not in the original phase list) | **Done** | SPEC.md §12, all three runtimes, live in production |
| 5 — Economic layer (payments/stake) | **Not started** | Deliberately deferred; real prerequisite (payment rail choice) is a product decision, not scoped here |
| 6 — Network/marketplace | **Not started** | Deliberately deferred |
| Verification v0.2 (backlog) | **Not started, sketched only** | `docs-design/verification-v0.2-backlog.md` — design notes, no code |

## What's actually done and verified, phase by phase

### Phase 1 — Hardening
D1 compare-and-swap fixes for receipt countersign/dispute, agent registration race fix, rate limiting (both runtimes), CORS policy, D1 foreign-key enforcement, reputation O(receipts) cost-amplification fix, real Worker test suite via `@cloudflare/vitest-plugin`.

### Phase 2 — Public protocol
GitHub org `inamprotocol` / repo `inamprotocol/inam-protocol` (public, Apache-2.0). `docs.inamprotocol.org` (rendered SPEC.md + Redoc API reference). `openapi.yaml`, versioned CHANGELOG.md with per-package sections. Root domain `inamprotocol.org` deployed (content-first landing page, `site/`) as part of this track's follow-through, though it happened later chronologically.

### Phase 3 — Job resource (SPEC.md §3)
`open → accepted → completed/cancelled`, offers as a separate table (avoids concurrent-offer races), receipt↔job consistency checks. All three runtimes, all SDKs.

### Phase 4 — External identity link challenges (SPEC.md §2.1)
`agentpass_id`/`aitp_id`/`passport_id` require a signed challenge (Ed25519 or P-256) proving control of the claimed key before linking; `a2a_endpoint` unaffected. Wire format aligned with ATTP (`draft-sharif-attp-00`, real IETF draft, confirmed via research — not assumed). A real cross-language bug was found and fixed here: Python's raw ECDSA P-256 signing didn't canonicalize to low-S, so it verified against the TS/Worker side only ~50% of the time; fixed with a regression test guarding it.

### Verification v0.1 (SPEC.md §12)
A single independent verifier's signed attestation that a finalized receipt's output satisfies its job's requirements. Scope locked with the user before implementation: one verifier per verification, `provider != verifier` enforced, only `deterministic`/`agent_attestation` methods, no new dispute mechanism (reuses the existing receipt dispute state — verified reputation contribution disappears automatically the instant a receipt is disputed, since reputation computation only iterates non-disputed `finalized` receipts). Reputation gets a weight boost + new `attestedReceipts` component, no second event ledger. All three runtimes; Worker port was done by a background fork while the Python SDK was built in parallel in the main session, then integrated and reviewed. Real cross-language proof: a receipt drafted in Python, finalized in TypeScript, then independently verified by a third TypeScript identity — live run showed `attestedReceipts` 0→2 and `trustScore` 8.7→12.4. D1 migration applied to production, Worker deployed, full smoke-test suite green against `https://api.inamprotocol.org`.

## Current live state (verify independently before relying on this — it decays fast)

- **GitHub**: `github.com/inamprotocol/inam-protocol`, public. **Branch protection on `main` is ON** (re-enabled 2026-09-07) — requires the 4 CI checks ("Node test suite (root)", "Worker test suite", "Python SDK test suite", "TypeScript typecheck") + `strict: true`, `enforce_admins: false`. Verify: `gh api repos/inamprotocol/inam-protocol/branches/main/protection`.
- **Deployments**: `https://api.inamprotocol.org` (registry API, Worker **v0.6.9 deployed 2026-09-07**, `Version ID: ab171298-4416-400f-b4e6-816d2fbcbadd` — first deploy carrying audit #9/#10/#11), `https://docs.inamprotocol.org` (spec + API reference), `https://inamprotocol.org` (landing page), **`https://explorer.inamprotocol.org`** (read-only public browser + live stats dashboard) — all live Cloudflare Workers. api verified 2026-09-07 (15/15 worker smoke checks green against prod, `linkedProof` in search response, `/revoke` + `/dispute/resolve` routes live).
- **SPEC.md**: v0.17 (Draft) — v0.17 (2026-09-07) is positioning-only (ERC-8004: §11 row + new §11.1), no code/wire change.
- **Production registry reset 2026-09-07**: the 58 pre-launch smoke-test agents (+ their receipts/jobs/offers/verifications/challenges) were deleted and replaced with 3 canonical reference agents (`Reference Extractor` / `Reference Reviewer` / `Reference Translator`) + one real finalized receipt between the first two (trustScore 5.5 each). Reference-agent private keys saved outside the repo (ask the user; not committed). Seed is reproducible: `scripts/seed-reference-agents.ts`. **Permanent fix for the recurrence: never point `scripts/*smoke-test.ts` at prod** — they default to localhost; a throwaway staging Worker+D1 is the place for live smoke runs.
- **All D1 migrations applied to production** (`migration-add-verifier-status.sql` #3, `migration-add-linked-proof.sql` #9, `migration-add-revocation.sql` #10 — all confirmed against the live `agents` table 2026-09-07). No pending migrations.
- **Package versions in the repo** (committed, built, tested) vs. **actually published**:
  | Package | Repo version | Published version (checked 2026-08-25) | Gap |
  |---|---|---|---|
  | `inamprotocol` (npm, `sdk-js`) | 0.3.1 | **0.3.1** | None — published via the new trusted-publishing workflow after a real bug (a critical cross-language canonical-JSON signature bug — see below) |
  | `inamprotocol` (PyPI, `sdk-python`) | 0.4.1 | **0.4.1** | None — same release, same fix |
- **2026-08-25/26: an external audit found 15 issues — all 15 are now fixed (closed #14 Worker half and #15 on 2026-09-17).** See the 2026-09-14/17 session note near the end of this file for detail.
  - **#1 (critical, live) — fixed & republished.** `score: 1.0` submitted via the Python SDK failed `INVALID_VERIFICATION_SIGNATURE` (server is always TypeScript; Python's `json.dumps` and JS's `JSON.stringify` disagree on number formatting for ordinary values). Live-reproduced before/after. `sdk-js@0.3.1`/`sdk-python@0.4.1` on npm/PyPI.
  - **#2 (P0) — fixed.** Node/Worker request-validation parity: the Worker only checked required fields were *present*, never valid (a signed `{"result":"banana","score":999}` passed on the Worker, was rejected by Node). Fixed by extracting every Zod schema into `sdk-js/src/core/schemas.ts` (one shared source, not two copies) and wiring both runtimes to it. Also fixed while in the same area: malformed JSON returned 500 instead of 400 on both runtimes (confirmed live, now 400 `INVALID_JSON` on both).
  - **#3 (P0) — fixed, SPEC.md bumped to v0.6, then corrected and completed at v0.10.** Verifier independence: self-verification guard excluded only the provider, not the requester (who could name itself "independent verifier" after already approving the same work); an unregistered `verifier` was never checked; the same verifier could submit contradictory `verified`+`rejected` records for one receipt. First two closed at v0.6 — requester now excluded too, new `VERIFIER_ALREADY_DECIDED` for a second decision. **The v0.6 "verifier must be a registered agent" check was itself insufficient — caught by the user, not the original audit**: registration is free and self-service, so it never restricted who could verify, only that they'd registered first, making verifier *count* meaningless as an independence signal. Corrected at v0.10: verifier eligibility is now an explicit operator grant — new `isAuthorizedVerifier` flag on `AgentRecord`, `false` by default, settable only by a single registry-configured operator identity (`INAM_OPERATOR_DID`/`OPERATOR_DID`, unset by default = locked down) via new `POST /agents/:id/verifier-status`; `submitVerification` now checks this flag (`VERIFIER_NOT_AUTHORIZED`), a non-operator grant attempt gets `NOT_OPERATOR`. New SDK methods (`setVerifierStatus`/`set_verifier_status`) in both languages. Live-proven end to end with a real operator keypair against a running server. Deliberately not done: a DB-level FK on `verifications.verifier` in `worker/schema.sql` — live-DB migration risk judged not worth it when the application-level check gives the same protection; flagged as a real follow-up. Also not done: operator succession/rotation/multi-operator support — a single static operator key is the deliberate v0.10 scope.
  - **#4 (P0) — fixed, SPEC.md bumped to v0.7.** Reputation inflation via future-dated receipts: the decay formula had no bounds on `result.completedAt`, so a future date produced a decay factor >1 (unboundedly more trust for claiming completion further in the future) instead of being rejected. Fixed: new `INVALID_TIMESTAMP` check (>5min future or before `createdAt`), strict ISO date-format validation (verified compatible with both TS's and Python's differing timestamp formats, live-tested with the real Python SDK), and decay clamped to `[0,1]` as defense for pre-existing data.
  - **#5 (P1) — fixed, SPEC.md bumped to v0.8.** Provider/requester score conflation: reputation is entirely role-blind, so two brand-new counterparties finishing one receipt get identical aggregate reputations regardless of role (live-reproduced: `examples/starter-agents.ts` still shows both at `trustScore: 8`). Deliberately did NOT build the audit's full proposed model (first-class `providerScore`/`requesterScore`/`verifierScore` — that's real scoring-model design work, P1 in the audit's own priority table, and `verifierScore` specifically overlaps with the already-deferred Verification v0.2 backlog). Instead added purely additive `components.asProvider`/`asRequester` (same weighting as the aggregate, filtered by role) so the signal is at least visible. Also documented (not renamed, since it's a live field): `verifiedReceipts` means finalized not independently-verified, and the ~80 asymptotic ceiling on `trustScore` without live staking.
  - **#6 (unranked in the audit's own P0/P1/P2 table, done in numbered order) — fixed, SPEC.md bumped to v0.9.** Reputation-boost exploit: `hasVerifiedAttestation` counted "at least one verified" among a receipt's Verifications, so a single verifier's `verified` outvoted any number of independent `rejected` records from other verifiers (1 verified + 9 rejected still counted as attested). Tightened to require verified to strictly outnumber rejected. **Explicitly stayed inside a previously user-agreed scope boundary**: this is a narrow anti-exploit tiebreak, not the "multi-verifier consensus" mechanism the user agreed to defer to v0.2 before Verification v0.1 shipped (CHANGELOG v0.5) and that SPEC.md §12.7 still lists as backlog — no verifier-trust weighting, no quorum, no new state added. Live-reproduced end to end (real server, real SDK, 3 verifiers).
  - **#7 (P1) — fixed, SPEC.md bumped to v0.11.** Currency conflation: `GET /agents/:id/reputation` summed every finalized receipt's `settlement.amount` into `components.volumeUsd` regardless of `settlement.currency` (1000 TRY added 1000 to a USD-labelled field, next to 25 USDC). `settlement.amount`/`currency` and a job's `budget` were also unvalidated strings — `{ "amount": "banana" }` reached the reputation math and `Number("banana")` → `NaN` poisoned the volume sums. Fixed without INAM taking on any FX/settlement role (stays out of scope, §10): new `components.volumeByCurrency` map (bucketed by normalized currency, never converted or cross-summed), `volumeUsd` redefined as exactly the `"USD"` bucket (a stablecoin like USDC is its own bucket, not USD), same split on `asProvider`/`asRequester`; `amount`/`currency` now regex-validated at the shared schema layer (`VALIDATION_ERROR`), with a non-finite/negative-amount guard in the computation as backstop for non-conformant-registry data. New shared `sdk-js/src/core/settlementVolume.ts` used by both runtimes. No DB migration (compute-only). Live cross-language proof: Python SDK drafted USD/TRY/USDC receipts against a running Node server → `volumeUsd` 100 (not 1125.5), `volumeByCurrency` `{USD:100,TRY:1000,USDC:25.5}`, lowercase normalized, malformed amount rejected 400. Versions: reference server/Worker 0.6.4→0.6.5, sdk-js 0.3.2→0.3.3 (type only), sdk-python unchanged.
  - **#8 (P1) — fixed, SPEC.md bumped to v0.12.** Replay/idempotency hardening: the signed-request string (`METHOD\npath\ntimestamp\nsha256(body)`) doesn't cover the `Idempotency-Key`, so a captured signed request replayed with a *fresh* key re-verified and missed the `(caller, key)` idempotency cache → duplicate side effect (clearest on `POST /jobs`) for the whole 5-min skew window. Fix (non-wire-breaking, SDKs untouched): `requireIdempotencyKey` in both runtimes now binds each verified signature to the one key it was first seen with and rejects the same signature + different key as `REPLAYED_REQUEST` (409); Node keeps it in a new `signatureReplayCache` (TTL = skew window), Worker in the KV namespace under a `replay:` prefix. Also: idempotency now caches **2xx only** (a transient 5xx/429 no longer pins the key), and the Node in-memory caches got TTL eviction + a bounded sweep (were unbounded). No DB migration. Live replay proof against a running Node server. Versions: server/Worker 0.6.5→0.6.6, sdk-js/sdk-python unchanged.
  - **#9 (P1) — fixed, SPEC.md bumped to v0.13.** External-identity verification depth: the `linked` map presented every external identity identically — a consumer couldn't tell an `a2a_endpoint` (bare URL, INAM-signed only) from a challenge-verified `agentpass_id`/`aitp_id`/`passport_id`, and the registry didn't even record which key a challenge proved or when. Fixed additively with a new `linkedProof` map on the agent record (`{ method: "key_possession" | "unverified_claim", verifiedAt, keyType?, externalPublicKey? }`), returned everywhere incl. `GET /agents/:id/protocols`. Deliberately does **not** add live cross-registry resolution (still out of scope, §10) — it makes the current (shallower) assurance level legible instead of leaving it to a doc paragraph. New Worker D1 column `linked_proof` → **`worker/migration-add-linked-proof.sql` must run against production D1 before deploying v0.6.7 Worker code** (same as the #3 verifier-status migration; `rowToAgent` tolerates the missing column on read, but `insertAgent` names it). `updateAgentLinked`→`updateAgentLinks` in worker db. Live cross-language proven (Python SDK against Node server). Versions: server/Worker 0.6.6→0.6.7, sdk-js 0.3.3→0.3.4 (types only), sdk-python unchanged.
  - **#10 (P1) — fixed, SPEC.md bumped to v0.14.** Key management: INAM had none — an INAM ID *is* its Ed25519 key, so a compromised/lost key left an identity permanently exposed or stranded. Added the minimal safe primitive: SPEC §2.2 `POST /agents/:id/revoke` *(signed, self)* — a one-way self-signed tombstone. Revoked ID → `revokedAt`/`revocationReason`, rejected from every further signed op (`AGENT_REVOKED` 403, enforced inside `requireSignedRequest` so it covers all signed routes with one check), dropped from `GET /agents/search` (`?include_revoked=true` opts in), flagged `revoked` in reputation. Finalized-receipt history untouched. **Deliberately NOT key rotation** (signed successor-chain + reputation migration) — deferred (§10). New `client.revoke()` in both SDKs. New Worker D1 columns `revoked_at`/`revocation_reason` (`worker/migration-add-revocation.sql`). Live cross-language proven. Versions: server/Worker 0.6.7→0.6.8, sdk-js 0.3.4→0.3.5, sdk-python 0.4.2→0.4.3.
  - **#11 (P1) — fixed, SPEC.md bumped to v0.15.** Job/dispute state-machine completeness. Three gaps: (1) `dispute.status` had a declared-but-unreachable `"resolved"` — a `disputed` receipt was a permanent dead end. New `POST /receipts/:id/dispute/resolve` (signed, dispute opener only) → `disputed → finalized`, `dispute.status → resolved` (+ `openedBy`/`resolvedAt`/`resolution`); reputation re-counts it, `in_dispute` clears; one-way (re-dispute → `DISPUTE_ALREADY_RESOLVED`). Not arbitration — third-party resolution stays §10 out-of-scope. (2) Node-only parity bug: `markCompletedByReceipt` allowed `cancelled → completed`; now guarded to `accepted → completed` only (matches Worker's existing CAS). (3) `expiresAt` never consulted → now `submitOffer`/`acceptOffer` reject an expired job with `JOB_EXPIRED` (lazy gate; auto status-transition still §10-deferred). New `client.resolveDispute()` in both SDKs. **No D1 migration** (dispute lives in the receipt `data` blob). Live cross-language proven. Versions: server/Worker 0.6.8→0.6.9, sdk-js 0.3.5→0.3.6, sdk-python 0.4.3→0.4.4.
  - **#12 (P1) — fixed, SPEC.md bumped to v0.16.** "No agent runtime" — the audit read §12's `deterministic`/`agent_attestation` verification methods as implying INAM owes a hosted runtime to execute the checks. It doesn't. Doc/scoping only, no code/wire/version change: §0's boundary now says a registry **MUST NOT** execute agent work *or verification logic* (it validates a signature, the operator grant, a hash match — nothing more); new §12.8 makes the verifier's own environment the normative place verification compute runs, at the verifier's cost (inline in its runtime, or a service it deploys itself — never the registry); §10 gains "a hosted execution or verification runtime". The reference implementations already conform. New `examples/reference-verifier.ts` (runnable, live-proven both with and without an operator grant). No D1 migration, no package bumps. Decision confirmed with the user first (2026-09-02): INAM bears no verification compute cost.
  - **3 remaining** (not started): privacy/access control, search/reputation scaling, doc/version drift. Being worked through in the user's own priority order.
- **Test coverage**: 82 Node vitest + 58 Worker vitest + 36 Python pytest = 176 automated tests, all green 2026-08-31 after the audit fixes above (through #11 job/dispute state-machine completeness).
- **Minor SDK follow-up noted during #11**: `disputeReceipt` / `resolveDispute` (both SDKs) use fixed idempotency keys (`dispute:<rid>` / `dispute-resolve:<rid>`), so a second call replays the first response rather than re-executing — a caller can't observe `DISPUTE_ALREADY_RESOLVED` through the SDK (the server enforces it correctly; the vitest suites cover it). Harmless for the one-dispute-per-receipt model but worth a `Date.now()`-suffixed key like the other write methods use, if touched again.

## Gaps and incomplete items found in this audit

1. ~~**PyPI publish is stuck, blocking.**~~ **Resolved, confirmed 2026-08-24.** PyPI shows `inamprotocol@0.4.0`, released 2026-08-22 — the venv retry did succeed, this file just hadn't been updated to reflect it. Noted separately below: the release was uploaded with plain `twine`, not PyPI Trusted Publishing — see the new CI/security item.
2. **npm is one version newer than what was last confirmed in conversation** — registry shows 0.3.0 live, meaning the user did publish it (previously confirmed here was 0.1.0, then it should have gone to 0.2.0, now 0.3.0). Not a problem, just noting the registry is ahead of the last explicit confirmation in this file — a reminder that "confirmed live" claims need re-checking, not trusted indefinitely.
3. ~~**OpenWork.network positioning**~~ **Resolved 2026-08-24.** Added as a row in SPEC.md §11 — flagged as an overlapping-claim, zero-public-repos, unverified-implementation competitor rather than a peer protocol to interoperate with.
4. **Verification v0.2 backlog** (`docs-design/verification-v0.2-backlog.md`) is a design sketch only. Explicitly not to be started without the user asking — flagging here only so it isn't mistaken for in-progress work.
5. **Phase 5 (payments/stake)** has a real, unresolved prerequisite that isn't a coding task: which payment rail `settlement.paymentRef` actually points at (x402, AP2, on-chain, something else). SPEC.md deliberately leaves this unenforced. This is a product/business decision, flagged in the v0.2 backlog doc too — worth a scoping conversation before any Phase 5 code gets written, not something to default into.
6. **`data/` and `.interop-tmp/` are gitignored and get wiped by nearly every test/demo run** — this is intentional (documented in README), not a gap, but worth stating explicitly so a future session doesn't mistake an empty `data/` directory for data loss.
7. ~~**No CI pipeline exists yet.**~~ **Resolved** — `.github/workflows/ci.yml` runs 4 jobs (Node/Worker/Python test suites + TS typecheck) on every push/PR to `main`. See the CI/CD audit below for the state as of 2026-08-30.
8. **npm SDK's `README.md`** (`sdk-js/README.md`) and the Python SDK's `README.md` — not re-audited in this pass for staleness against the latest Verification-resource additions; worth a quick check next time either SDK is touched, since neither was specifically reviewed for the Verification-era changes' documentation completeness (as opposed to the root README/CHANGELOG/SPEC, which were all updated).
9. **Neither package uses provenance/trusted publishing.** `inamprotocol@0.4.0` on PyPI was uploaded with plain `twine` (no PyPI Trusted Publishing / OIDC); npm side likewise has no `npm publish --provenance`. Both require the user's own action on the npm/PyPI project settings pages (this session can write the GitHub Actions workflow side, not flip the account-level toggle) — flagged, not started.
10. **Zero external-adoption signal** — 0 GitHub stars/forks/issues/releases as of this writing, no example integrations, no quickstart shorter than reading the full README. This isn't a code gap, it's *the* current bottleneck: the protocol works (92/92 tests, all three domains live) but nothing outside this repo has used it yet.

## CI/CD end-to-end audit (2026-08-30)

Full pass over `.github/`, branch protection, publish + deploy paths.

**Fixed this session (commit alongside audit #8 follow-up):**
- **Deprecated runners** — every workflow pinned `actions/checkout@v4` / `actions/setup-node@v4` / `actions/setup-python@v5`, all targeting the now-deprecated Node 20 (GitHub forces Node 24, warns on every run, will hard-fail later). Bumped to `checkout@v5` / `setup-node@v5` / `setup-python@v6` across `ci.yml`, `publish-npm.yml`, `publish-pypi.yml`.
- **`npm install` → `npm ci`** in `ci.yml` (all jobs) — enforces lockfile, faster, reproducible. Lockfiles verified in sync (`npm ci --dry-run` clean at root + sdk-js + worker).
- **`ci.yml` gained `workflow_dispatch`** — can now be re-run manually without a dummy push.
- **`publish-npm.yml` now runs the root vitest suite** (the suite that actually exercises sdk-js's crypto/canonical code) before publishing — previously only `tsc` build ran, so a `workflow_dispatch` publish from a dirty local checkout could ship a regression CI would have caught.
- **Added `.github/dependabot.yml`** — weekly, grouped: github-actions, npm (all 6 package dirs), pip. Was absent entirely.

**Still open — needs the maintainer (account/admin access this session doesn't have):**
- ~~**Branch protection on `main` is OFF**~~ **Done 2026-09-07** — re-enabled with the 4 CI checks + `strict: true` (see "Current live state").
- **npm/PyPI Trusted Publishing account toggles** — the GitHub Actions side is done (OIDC, no token secrets); the one-time account-side config on npmjs.com and pypi.org is still pending (`docs-design/trusted-publishing-setup.md` has the exact fields). Until then a `workflow_dispatch` publish will fail at the OIDC exchange.

**Reported, not changed (design decisions, not bugs):**
- **No CD for the 4 Cloudflare Workers** (`api`, `docs-site`, `explorer`, `site`) — all deploy by hand via `wrangler deploy`. No deploy workflow, no post-deploy smoke check (`scripts/worker-smoke-test.ts` exists but isn't wired to anything). Deliberate so far (single maintainer controls deploy timing); worth an `on: workflow_dispatch` deploy workflow per Worker if that changes.
- **No CodeQL / dependency-review / secret-scanning workflow.** `SECURITY.md` + the `security-review` skill cover the manual path.
- **The `release`-triggered publish path is untested** — zero GitHub releases/tags exist, so `publish-*.yml` has only ever been reachable via `workflow_dispatch`. The version-vs-tag `check` job logic is sound on inspection but unproven against a real release event.
- **No lint/format step** — no ESLint/Prettier/Biome config in the repo at all. Typecheck + tests are the only automated quality gates. Fine for a small codebase; add if contributor count grows.

## Next action

2026-08-24 direction-setting: three external LLM reviews (ChatGPT, DeepSeek, Qwen) independently converged on the same read — the protocol core is done and verified (confirmed again today: 92/92 tests green, api/docs/site all live), and the real bottleneck from here is adoption, not more protocol surface. Agreed with that read; STATUS.md's own phase table already had "5 — Adoption/Network: Not started" before this session. Decided **not** to start Phase 5/6/7 protocol features (network directory, payments, federation) — those still need the payment-rail product decision from gap 5 above — and instead to close the adoption *prerequisites* that were sitting as open gaps in this very file: CI (gap 7), SECURITY.md/CONTRIBUTING.md/provenance groundwork (gap 9), a real 5-minute quickstart, and one concrete "add INAM to an existing agent" framework-integration example. Two background agents were dispatched same-session to draft these (isolated git worktrees, not yet merged — see PR/branch names in the next session's `git branch` output if this file wasn't updated again after they landed).

Rough priority order from here:
1. Review and merge whatever the two dispatched agents produced (CI+security track, quickstart+integration-example track).
2. Whenever the user wants: Phase 5 scoping conversation (payment rail decision), Verification v0.2, npm/PyPI trusted-publishing toggle (needs the user's own account access), or an actual adoption push (posting the quickstart somewhere developers will see it).

---

### 2026-09-07 session: P0 credibility pass + adoption push started

User directive: move fast, get to external users. Done this session:
- **Branch protection ON** (was the "highest-value open item").
- **Both pending D1 migrations applied to prod + Worker v0.6.9 deployed** — prod now matches the repo (audit #9/#10/#11 are live for the first time). 15/15 prod smoke checks green. This closed the single worst problem: the site advertised v0.16 while the live API was still pre-#9.
- **Production registry reset** — 58 smoke-test agents → 3 canonical reference agents + 1 real finalized receipt. `scripts/seed-reference-agents.ts` is the reproducible seed; keys held by the user, not committed.

Next, in order (adoption, not protocol surface):
1. **`inam-mcp` package** — **built 2026-09-07** in `mcp/` (standalone MCP server over stdio, deps `@modelcontextprotocol/sdk` + `inamprotocol@^0.3.1`). 4 read tools (keyless: check_reputation, search_agents, get_receipt, whoami) + 6 write tools gated on `INAM_PRIVATE_KEY` (register, post_job, submit_offer, accept_offer, submit_receipt, countersign_receipt). Both smoke tests green: `mcp/test-smoke.mjs` (read, vs prod), `mcp/test-write.mjs` (full job lifecycle, vs local dev). `npm pack` clean (README + dist/index.js + package.json, 4kB). **Still to do: user runs `npm publish` from `mcp/` (their account); then submit to the official MCP Registry.** Recommend also publishing `inamprotocol@0.3.6` (5 versions of audit fixes unpublished; npm still has 0.3.1) but inam-mcp doesn't need it.
2. **ERC-8004 interop demo** + a one-pager "why not ERC-8004" (SPEC §11 → prose).
3. **Self-host story to the front page** — `docker compose up` / "Deploy to Cloudflare" button; the SDK already takes a base URL.
4. ~~Quickstart 205 → ~10 lines~~ **done 2026-09-07** — `QUICKSTART.md` rewritten to ~20 lines against the live API (`npm i inamprotocol` → register→job→receipt→countersign→reputation), tested end-to-end against prod with the published SDK. Local-registry path kept as a secondary section.
5. Hand-written integrations: pick ~10 open-source agent projects, open the PR that adds an INAM receipt yourself.

Kill criterion agreed in principle (not yet dated by the user): if no receipt from a key that isn't the maintainer's appears within a set window, freeze the protocol as-is, archive, publish the spec, move on.

### 2026-09-12 session: coding-agent-verification example

Built `examples/coding-agent-verification.ts` (+ README entry) as prep for item 5/2 above: uses the existing Job→Receipt→Verification flow (no new protocol surface) to demonstrate catching a self-reported "tests pass" that lied, using the current "developers don't trust AI coding output" pain point (84% use AI coding tools, only 29% trust the output) as the framing hook. Two rounds against a real local server: a subtly-buggy `isPalindrome()` self-reports success and gets finalized on faith, then an independent verifier actually runs a broader test suite and rejects it (`attestedReceipts` stays 0); a fixed version passes the same independent check and is verified (`attestedReceipts` → 1). Verified live both without an operator key (signs but doesn't submit, like `reference-verifier.ts`) and with one (full submit path) — reputation numbers behaved exactly as expected in both runs.

Not yet done: picking the actual target repo(s) for a real integration PR (item 5) — this example is the pattern to adapt, not the PR itself. A real autonomous-coding-agent open-source project (not a generic agent framework) is the better fit for this specific angle than the general ~10-project list.

### 2026-09-12 session (cont'd): closed every "built but not published" gap that didn't need a human credential

- **`inam-mcp` is now live in the official MCP Registry** (`io.github.inamprotocol/inam-mcp`, confirmed via `GET /v0/servers?search=inamprotocol`). It was already on npm (published 5 days prior — STATUS.md's old "still to do: npm publish" line was stale). The registry publish needed two things neither existed before: (1) the `inamprotocol` GitHub org's membership had to be made public (`PUT /orgs/inamprotocol/public_members/hheskihoran` — confirmed by the user first, since it's a visible profile change; fully reversible), and (2) org namespaces can't be proven by a personal `mcp-publisher login github` (that only ever grants `io.github.<personal-username>/*`) — added `.github/workflows/publish-mcp.yml` (GitHub Actions OIDC, same trusted-publishing pattern as npm/PyPI) and ran it via `gh workflow run` to actually publish. Also fixed `mcp/server.json`'s `description` (was 104 chars, registry caps at 100).
- **`/use-cases` page added to the site** (`site/public/use-cases.html`, linked from nav on both pages), deployed live. Four cases, each linking to a real file in `examples/`: the new coding-agent-verification example, `starter-agents.ts`, `reference-verifier.ts`, and the MCP/LangChain/raw-HTTP integration snippets (plus the now-published `inam-mcp`).
- **`git push`ed everything** that was sitting locally (coding-agent-verification example, the mcp workflow, the site page) — branch protection required a bypass notice (4 required checks hadn't run yet at push time) but CI went green on `main` immediately after for every push. `origin/main` now matches local.
- **npm/PyPI publish gap: resolved** (confirmed live 2026-09-14) — `inamprotocol` is **0.3.6 on npm** and **0.4.4 on PyPI**, both matching the repo exactly. The user published both themselves after the OTP/token blockers above; this section's old "still blocked" framing is stale, kept below for history only.
  - ~~**npm**: `inamprotocol` (sdk-js) is published at 0.3.1; the repo is at 0.3.6~~ — user ran `npm publish --access public` past the OTP prompt themselves.
  - ~~**PyPI**: `inamprotocol` (sdk-python) is published at 0.4.1; repo is at 0.4.4~~ — user ran `twine upload` with their own token.
- **GitHub issues**: none exist (checked `gh issue list` — zero open or closed). Dependabot PRs: **11 open as of 2026-09-14** (grew from 9 — new npm-minor-patch group PR #13; major-version bumps zod 3→4/5, TypeScript 5→7, `@types/node`, `express` still unreviewed). Left untouched — a major-version dependency bump needs real review/testing, not a blind merge.

### 2026-09-14/17 sessions: audit #13 shipped + published; audit #14 (Node half) done

- **Audit #13 (privacy/access control, SPEC v0.19)** and the **secp256k1/erc8004_id link protocol (SPEC v0.18)** shipped, deployed (Worker `238be8de`), and published for real (`inamprotocol` npm 0.4.1, PyPI 0.5.1 — confirmed live on the registries, npm carries provenance + trusted-publisher attestation). npm/PyPI Trusted Publishing (GitHub Actions OIDC, `publish-npm.yml`/`publish-pypi.yml`) is fully working end-to-end — no more manual token/OTP step.
- **Audit #14, Node half — done.** `src/storage/jsonStore.ts` (in-memory Map + full-file rewrite on every write, every list a linear scan) replaced with `src/storage/db.ts` on `node:sqlite` (WAL mode, indexed tables). All `.all().filter()` call sites in `agentService`/`jobService`/`receiptService`/`verificationService` now go through indexed queries; dead `jobService.listByPoster` removed. New `tests/storageQueries.test.ts` asserts `EXPLAIN QUERY PLAN` shows index use, not `SCAN`. Root reference server bumped to **0.7.0** (Worker stays 0.6.11 unchanged — D1 was already indexed). CI's root Node job + `publish-npm.yml` bumped to Node 24 for `node:sqlite`; Worker/Python jobs stay on 22/3.11. `node:sqlite` is imported via `process.getBuiltinModule("node:sqlite")` rather than a static `import "node:sqlite"` — Vite 5's node-builtin allowlist predates that module and otherwise fails resolving it under vitest. Node 92/92, Worker 63/63, proven live against a running server via `npm run demo` (real `registry.db`+WAL files on disk, full job→receipt→reputation flow).
- **Worker half of #14 — done, deployed 2026-09-17 (Version ID `84c7f795`).** New `agent_capabilities(agent_id, capability)` junction table + index; `searchAgents` now joins it instead of `allAgents` + full-scan filter. `insertAgent` populates it at registration (batched, capabilities never mutate after registration). Migration ran against prod D1 first (user ran it via `!`), confirmed live: `agent_capabilities` has 7 backfilled rows matching the prod registry, `EXPLAIN QUERY PLAN` on prod shows `SEARCH ... USING INDEX` on both joined tables, live capability search returns correct results. Worker 64→65 tests. Version 0.6.12→0.6.13. **Audit #14 is now fully closed (both runtimes).**
- **Wash-trading cap — done, SPEC v0.20 (2026-09-17).** `computeReputation` (both runtimes) now zero-weights a counterparty's receipts beyond `floor(threshold/(1-threshold) * otherReceipts)` once an agent has ≥3 finalized receipts. Live-proven: the exact 15-receipts/5.5→21 scenario the independent review found now flatlines at 0 from the 3rd receipt on. Node 93/93, Worker 64/64. Versions: server 0.7.0→0.7.1, Worker 0.6.11→0.6.12.
- **`verification.method` naming-collision cleanup — done, SPEC v0.21 (2026-09-17), docs-only.** §4.1's receipt-level `verification.method` and §12's `Verification.method` share a name but are unrelated enums — an independent reviewer actually fell into this. Fixed with cross-referencing SPEC prose, JSDoc on both TS type declarations (all three runtimes' type files), and `openapi.yaml` `description` fields (+ a fleshed-out `Receipt.verification` schema, was untyped). No rename — a wire-level rename was considered and rejected as disproportionate, especially for a field already live in two published SDKs. No code/wire/version change.
- **Audit #15 (doc/version drift) — done, 2026-09-17.** The docs-site version-badge fix (2026-09-14) only closed one instance. Found the same bug live on `inamprotocol.org`: `site/public/index.html` hardcoded `v0.17` (SPEC was at v0.21) plus three stale package versions in its footer (`registry 0.6.9`, `js 0.3.6`, `py 0.4.4` — actual: Worker 0.6.13, sdk-js 0.4.1, sdk-python 0.5.1), and unlike docs-site, `site/` had no build step at all (`wrangler.jsonc` served `public/` directly) and no CD workflow. Fixed with the same discipline as the docs-site fix: `public/index.html` is now a template with `{{SPEC_VERSION}}`/`{{SPEC_STATUS_LOWER}}`/`{{REGISTRY_VERSION}}`/`{{JS_VERSION}}`/`{{PY_VERSION}}` placeholders, new `site/scripts/build.mjs` derives all five from `SPEC.md`, `worker/package.json`, `sdk-js/package.json`, `sdk-python/pyproject.toml` and writes `site/dist/` (gitignored, `wrangler.jsonc` now points `assets.directory` at it instead of `public/`); new `.github/workflows/deploy-site.yml` auto-deploys on a push touching any of those four source files or `site/**`, mirroring `deploy-docs-site.yml`. No new dependencies (stdlib `node:fs`/`node:url` only). Verified locally: `node scripts/build.mjs` correctly produces `v0.21 Draft` / `0.6.13` / `0.4.1` / `0.5.1`, zero leftover `{{` placeholders.

### 2026-09-17 session (cont'd): seven external AI reviews landed in `reviews/`, dispute-fairness fix shipped (first item)

- Right after the 15-item audit closed, the user ran the live site/repo through seven independent AI reviews (Claude, DeepSeek, Gemini, Qwen, Perplexity, "useai", and a Word report), all dropped in the gitignored-from-attention `reviews/` folder. Heavy overlap between them (most restate already-fixed items or general Sybil/positioning concerns), but two — the first Claude-authored one and `useai.txt` — did real local reproduction and surfaced genuinely new, concrete findings not on the original 15-item list. Full list triaged and ordered with the user; agreed order: (1) dispute fairness, (2) verifier-revoke historical check, (3) Sybil-ring bypass of the wash-trading cap, (4) negative-outcome path, (5) small hardening batch (draft spam/countersign-blind-signing/participants_only leak via job record/small-order did:key/Math.random() job id/replay base64-tolerance/signing-string host binding), (6) append-only transparency log (architectural, biggest lift), (7) verify whether `/jobs/search` actually 500s past 100 jobs (audit #14 didn't touch job search pagination).
- **Item 1, dispute fairness — done, SPEC v0.22.** Two bugs in `src/services/receiptService.ts`/`worker/src/receiptService.ts`'s `openDispute`/`resolveDispute`, confirmed by reading the code directly (not just trusting the review): (a) **self-immunization exploit** — `dispute.status === "resolved"` was one shared gate for both parties, so a worker could dispute its own just-finalized receipt and immediately withdraw it (`resolveDispute` is opener-only, needs no cooperation), permanently blocking the requester's real dispute with `DISPUTE_ALREADY_RESOLVED`. Fixed: new `dispute.usedBy` (DID array) tracks each party's one-shot dispute right separately. (b) **free-forever hostage** — an open dispute the opener never resolved excluded the receipt from reputation permanently at zero cost. Fixed: new `dispute.resolutionDeadline` (same duration as the dispute-opening window, from open time); past it, an unresolved dispute stops counting as active for reputation (receipt rejoins the positive side, `in_dispute` clears) though it stays formally resolvable later.
- New shared `sdk-js/src/core/disputeLifecycle.ts` (`hasUsedDisputeRight`, `isDisputeActive`) — same storage-free pattern as `receiptVisibility.ts`, imported by both runtimes' `receiptService.ts` and `reputationService.ts`. Both new fields additive/optional on `dispute`; **no D1 migration** (receipts store as an opaque JSON blob in both runtimes, confirmed by reading `worker/src/db.ts`'s `disputeReceiptIfFinalized`/`resolveDisputeIfDisputed` before starting). No SDK client-method change; `sdk-python` needs none (dict passthrough, no typed receipt dataclass).
- Tests: 2 new in `tests/receiptFlow.test.ts` (Node) — the immunization-exploit repro (worker self-disputes-and-resolves, requester's real dispute still succeeds; both parties' rights correctly spent after) and the deadline-expiry repro (`vi.useFakeTimers`, jump 73h past a 72h deadline, receipt rejoins reputation). 1 new in `worker/tests/api.test.ts` (HTTP-level immunization repro via the real Hono app, `call()` helper) — skipped a matching Worker-side timer test since Miniflare's isolate clock doesn't share Node's fake-timer state, and the underlying `isDisputeActive` logic is the same shared module already covered by the Node-side test. Node 93→95, Worker 65→66, both `npm run build`/`typecheck` clean.
- **Item 2, revoked-verifier boost — done, SPEC v0.23.** `hasVerifiedAttestation` (both runtimes) checked a Verification record's `result` only, never re-checking `isAuthorizedVerifier` at computation time — so revoking a verifier (`setVerifierStatus(..., false)`) didn't stop its already-submitted `verified` record from continuing to apply the 1.5x attestation boost, confirmed by reading `src/services/verificationService.ts`/`agentService.ts` directly (no point-in-time authorization history existed to check against, so "currently authorized" is the only signal available). Fixed: both `hasVerifiedAttestation` implementations now filter Verification records to currently-authorized verifiers before computing the `verified`-vs-`rejected` majority — applied to both sides of the tiebreak (a revoked verifier's `rejected` record stops counting too, for consistency, not just its `verified` ones). Worker version needed a plain `for` loop instead of `.filter()` since `getAgent` is async there (an async predicate inside `Array.filter` silently never filters — caught before shipping). Deliberately did not add an authorization-history log (the review's secondary suggestion) — current-status semantics directly close the reported bug without new persisted state; same conservative-default philosophy the protocol already applies to disputed receipts. New tests in both runtimes (submit verified while authorized → `attestedReceipts: 1` → revoke → `attestedReceipts: 0`). Node 95→96, Worker 66→67, both green, typecheck clean. Versions: SPEC v0.23, server 0.7.3, Worker 0.6.15 (sdk-js unchanged — no type/client-surface change).
- **Shipped, deployed, published — confirmed live.** Committed `45cd6b1`, pushed to `main` (CI green, `Deploy site`/`Deploy docs-site` auto-redeployed on the version-file changes). Worker deployed (`Version ID 5cc535a2`), sanity-checked against `api.inamprotocol.org` post-deploy (`GET /v1/agents/search` responds correctly). `inamprotocol` npm published for real at 0.4.2 via `gh workflow run publish-npm.yml` (Trusted Publishing/OIDC + Sigstore provenance) — took a few minutes for the registry to serve the new version/tag after the workflow reported success, confirmed once `registry.npmjs.org/inamprotocol/latest` returned `0.4.2`. sdk-python untouched, no publish needed.
- **Item 3, Sybil-ring bypass of the wash-trading cap — done, SPEC v0.24, shipped and deployed.** The wash-trading cap (SPEC v0.20) only limits volume from a single over-represented counterparty; a ring of many low-volume sockpuppet counterparties (each individually under the concentration threshold) stays invisible to it. Added `isAnchoredCounterparty(agentId, excludeIds)` to both `src/services/reputationService.ts` and `worker/src/reputationService.ts`: an agent has "standing" if it has stake, or any finalized/reputation-countable receipt with a counterparty outside `excludeIds` (the scored agent + everyone *it* has ever transacted with). In `computeReputation`, if the fraction of an agent's finalized receipts whose counterparty fails this anchored check exceeds the 0.6 concentration threshold (same as the per-pair check), pushes a new flag `unanchored_counterparty_volume`. **Deliberately flag-only, not a weight cap** — a brand-new legitimate counterparty is indistinguishable from a ring member by this one-hop check alone at the moment of a first transaction; a weight cap was tried and reverted after it zeroed out ordinary few-receipt cold-start scores (most of the live registry today). Full reasoning in the function's doc comment.
  - **Worker port** needed a plain `for` loop instead of `.some()`/`.filter()` — same async-predicate gotcha hit during item 2 (`getAgent`/`listByAgent` are async on the Worker; an async predicate inside `.filter()` silently never filters).
  - New repro tests in both runtimes ("flags a Sybil ring spread across many counterparties..."): 5 sockpuppet feeders × 3 receipts each with one target, none individually concentrated (20% each, well under 60%) so `concentrated_counterparty` stays silent, but 100% of the target's volume is unanchored so the new flag fires. Node 96→97, Worker 67→68, both green; root `npm run build` and Worker `tsc --noEmit` both clean.
  - SPEC.md → v0.24, CHANGELOG.md updated (protocol + server/worker sections), server `package.json` 0.7.3→0.7.4, Worker `package.json` 0.6.15→0.6.16. sdk-js/sdk-python untouched — flag is a plain string in the existing `flags: string[]` array, no schema/type change, no publish needed.
  - **Shipped, deployed, confirmed live.** Committed `1c20520`, pushed to `main` (CI triggered, `Deploy site`/`Deploy docs-site` auto-redeployed on the version-file changes). Worker deployed by the user (`Version ID 1b37eb89-23ef-4b5d-bbb7-ec0c118ec39e`); sanity-checked against `api.inamprotocol.org` post-deploy (`GET /v1/agents/search` 200 with real data, `GET /v1/agents/:id/reputation` 200 on a real reference agent — the new `isAnchoredCounterparty` code path runs clean on live data, though the live registry doesn't yet have any agent with ≥3 finalized receipts to exercise the flag itself). sdk-js/sdk-python untouched, no publish needed.
- **Item 4, no negative-outcome path — done, SPEC v0.25, shipped and deployed.** An Execution Receipt requires the worker's own signature (§4.1), so a worker who ghosts an accepted job left no record at all — success rate stayed structurally ~100% since only completed work ever produced a receipt. New **§3.3**: `POST /jobs/:id/report-nonperformance` *(signed, poster only)*, callable once the dispute-resolution-length grace window (72h) has passed since `acceptedAt` with no finalized receipt. New `JobRecord` fields `acceptedAt` (set at acceptance) and `nonPerformance` (set on report), new terminal `JobStatus` value `nonperformed` (blocks `cancelJob` like the other terminal states).
  - **Reputation impact**: `computeReputation` (both runtimes) folds each report in as a zero-outcome contribution weighted by the *reporting poster's own* base trust, deduped to one per unique poster — repeat reports from a single requester against the same worker don't compound, deliberately closing the mirror-image of the wash-trading bug (v0.20/v0.24) before it could ship as a new exploit. A malicious poster still can't *forge* a worker's offer (`submitOffer` requires the worker's own signed request), so this only works against a worker who genuinely engaged. New `components.nonPerformanceReports`, flag `nonperformance_reported`.
  - **Worker needs a D1 migration before deploy**: `worker/migration-add-nonperformance.sql` adds `accepted_at`/`nonperformance_reported_at`/`nonperformance_reason` columns + an index on `(status, accepted_agent_id)` to the `jobs` table. Node reference server needs none (jobs are an opaque JSON blob).
  - New `client.reportNonPerformance()`/`report_non_performance()` in both SDKs — genuinely new public API surface, so both got minor bumps (`sdk-js` 0.4.2→0.5.0, `sdk-python` 0.5.1→0.6.0), not patch. `openapi.yaml` updated (new path + `Job`/`ReputationComponents` schema fields).
  - New regression tests in both runtimes covering the full gating chain (still-open rejected, wrong-caller rejected, too-early rejected, success after the grace window, one-shot re-report rejected, terminal state blocks cancel) plus the reputation-folding/dedup behavior. Node 97→99, Worker 68→70. Worker's fake-timer limitation (noted under item 1) meant the grace-window test backdates `accepted_at` directly via `env.DB` instead of `vi.useFakeTimers`. Node 99/99, Worker 70/70, both `npm run build`/`tsc --noEmit` clean, sdk-js `npm run build` clean, sdk-python 45/45 pytest green.
  - SPEC.md → v0.25, CHANGELOG.md updated (protocol + sdk-js + sdk-python + server/worker sections), server `package.json` 0.7.4→0.7.5, Worker `package.json` 0.6.16→0.6.17.
  - **Committed (`5138bb8`), pushed, migration + deploy done by the user, confirmed live.** D1 migration ran clean against prod (4 queries, 7 rows written). Worker deployed (`Version ID 14f74139-dbf4-4f33-ade0-18de17605b56`). Sanity-checked: `GET /v1/agents/search` 200, `GET /v1/agents/:id/reputation` 200 with the new `nonPerformanceReports: 0` field present, and `POST /jobs/nonexistent_job_id/report-nonperformance` correctly returns `401 MISSING_SIGNATURE` (route wired, no 500) — no real job/receipt data created against prod, per the standing rule against pointing smoke tests at it.
  - **Published, confirmed live.** `gh workflow run publish-npm.yml`/`publish-pypi.yml` both completed successfully (Trusted Publishing/OIDC, no manual token step). Confirmed via the registries directly: `registry.npmjs.org/inamprotocol/latest` → `0.5.0`, `pypi.org/pypi/inamprotocol/json` → `0.6.0` (PyPI updated first; npm took a few minutes to propagate, same as past publishes). Item 4 is now fully shipped end to end.
- **Item 5, small hardening batch — done, SPEC v0.26, shipped and published.** Six of the seven findings fixed; the seventh (signing-string host binding) is a genuine wire-breaking change deferred pending a design decision (what literal value represents "this registry" across the `api.inamprotocol.org` custom domain + `*.workers.dev` dual-hosting setup) — raised to the user as a separate decision point, not yet resolved.
  - **Small-order Ed25519 key rejection.** A degenerate public key registers as a valid `did:key` with no rejection anywhere; for such a key Ed25519 verification is satisfiable by arbitrary signature bytes with no private key at all. Fixed in `sdk-js/src/crypto/keys.ts`'s `verify()`/`verifyRawEd25519()` (`Point.fromHex(key).isSmallOrder()`, `@noble/ed25519` v2.3.0) — closes registration, receipt/verification signatures, and link-challenge proofs at once, since every raw-key verification in this codebase routes through these two functions. Ported to `sdk-python/inamprotocol/keys.py` too, as a fixed 8-entry blacklist of the curve's known low-order points (edwards25519's cofactor is 8) — derived and cross-checked against `@noble/ed25519`'s own `isSmallOrder()` via a small Node script rather than trusted from memory, since a wrong blacklist entry would be worse than none.
  - **Replay-guard base64 non-canonical re-encoding.** The replay guard hashed the raw `inam-signature` header *string*; a base64-encoded 64-byte signature has unused low bits in its final character that a decoder ignores, so a captured signature can be re-encoded into a different string decoding to the identical bytes, bypassing the guard (reproduced: one captured request minted many extra jobs this way). Fixed in `src/middleware/idempotency.ts`/`worker/src/idempotency.ts` — hash the *decoded* signature bytes (`fromBase64(...)`), not the header string.
  - **Draft-receipt spam / unconsented public exposure.** A `draft` receipt (only agentB's own claim) was public and countable regardless of its own `visibility` — anyone could name any registered agent as `agentA` on an unbounded number of public drafts with zero involvement from that agent. Extended the shared `isReceiptRestricted()` (`sdk-js/src/core/receiptVisibility.ts`) to also restrict `status === "draft"` regardless of `visibility` — closes `GET /receipts/:id`/`GET /agents/:id/receipts` in both runtimes with **no route-layer change**, since both already route through this shared function. `rawReceipts` (`src/services/reputationService.ts`/`worker/src/reputationService.ts`) now excludes drafts too.
  - **`participants_only` leak via the linked Job record.** `GET /jobs/:id`/`GET /jobs/search` exposed `postedBy`/`acceptedAgentId`/`budget` unconditionally even once a job's linked receipt was `participants_only`, fully defeating that receipt's own visibility setting. New SPEC.md §3.4: a completed job inherits its linked receipt's participants-or-attesting-verifier gate. Implemented in `src/routes/jobs.ts` (new `isJobVisible()`, reusing `receiptService.isReceiptVisible` — same pattern `receipts.ts` already uses) and `worker/src/index.ts` (async mirror, reusing the existing `callerIsVerifierOf`). New error code `JOB_NOT_VISIBLE` (403 on `GET /jobs/:id`, silent filter on `GET /jobs/search` — same "filter don't error" convention as receipt listings).
  - **Job ID predictability.** `generateJobId()` in both `src/services/jobService.ts`/`worker/src/jobService.ts`: `Math.random()` → `crypto.randomUUID()`. Job ids were never meant to be secret (jobs are discoverable by design), but predictability had no upside either.
  - **Countersign blind-signing.** `sdk-js`'s `acceptWork()` signed whatever receipt content it was handed, zero validation — a real risk for a caller (e.g. an LLM-driven agent reachable via prompt injection through a job's or dispute's free-text fields) that fetches a draft and signs it without independently re-confirming its content. Fixed: `acceptWork()` now refuses to sign a receipt whose `agentA.id` isn't the calling client's own `did`, and accepts an optional second argument `expected: { jobId?, outputHash?, amount?, currency? }` validated against the fetched draft before signing when supplied — the caller states what it believes it's approving, sourced from its own knowledge, not from the untrusted fetched object. Mirrored in `sdk-python/inamprotocol/client.py`'s `accept_work()`. The concrete exploitable surface, `mcp/src/index.ts`'s `inam_countersign_receipt` tool, now takes **required** `expectedJobId`/`expectedOutputHash` tool parameters and passes them through — an LLM calling this tool must now state what it's approving as part of the call itself, not just a bare `receiptId`. `mcp/README.md` and `mcp/test-write.mjs` updated to match.
  - **Deliberately not fixed for real cross-registry protection**: a poster fabricating a job purely to build up non-performance reports, or other multi-hop graph-analysis-requiring attacks — out of scope for this batch, same boundary as the existing Sybil-ring flag.
  - New regression tests reproducing every finding directly: Node `tests/hardening.test.ts` (6 new tests: small-order rejection, base64-tolerance replay, draft-spam visibility + rawReceipts exclusion, countersign-guard × 2 unit tests, participants_only job leak), Node 99→105. Worker `worker/tests/api.test.ts` (4 new describe blocks mirroring the HTTP-level ones), Worker 70→74. `sdk-python/tests/test_hardening.py` (3 new tests: small-order rejection, accept_work agentA guard, accept_work expected-mismatch guard) plus one existing `test_visibility.py` fixture fixed to use the client's own `did` as `agentA` (the new guard correctly rejected the old fixture's mismatched placeholder DID) — sdk-python 45→48. Node `npm run build`/typecheck clean, Worker `tsc --noEmit` clean, `sdk-js/npm run build` clean.
  - SPEC.md → v0.26 (new §3.4, updated §2/§4.3/§4.4/§5.3/§6/§7/§8), CHANGELOG.md updated (all four sections), `openapi.yaml` updated (`GET /jobs/:id`/`GET /jobs/search` descriptions + 403 response). Versions: server `package.json` 0.7.5→0.7.6, Worker `package.json` 0.6.17→0.6.18, `sdk-js` 0.5.0→0.6.0 (minor — new `acceptWork` signature + real behavior changes to existing exports), `sdk-python` 0.6.0→0.7.0 (minor, same reasoning). `mcp/package.json` 0.1.0→0.2.0 and `mcp/server.json` 0.1.0→0.2.0 (both entries) — the tool's required-parameter change is a real interface change for any existing caller. `mcp/package.json`'s `inamprotocol` dependency bumped `^0.3.1`→`^0.6.0` (mcp had been pinned 5 versions behind on a range that, being pre-1.0 caret semantics, wouldn't have picked up the new version automatically anyway).
  - **No D1 migration needed** — every fix above is logic-only against existing columns/fields.
  - **Shipped, committed, pushed, published — confirmed live.** Committed `e652edf`, pushed to `main` (CI/`Deploy site`/`Deploy docs-site` all green). `gh workflow run publish-npm.yml`/`publish-pypi.yml` both completed successfully. Confirmed via the registries directly: `pypi.org/pypi/inamprotocol/json` → `0.7.0` (live within seconds), `registry.npmjs.org/inamprotocol/latest` → `0.6.0` (took a few minutes to propagate, same as every prior npm publish this session). Once npm showed `0.6.0`, ran `npm install && npm run build` inside `mcp/` against the *real* published package (not locally symlinked source) — confirmed the new two-argument `acceptWork(receipt, expected?)` type actually resolves and `mcp/src/index.ts`'s call site typechecks clean; this was the one thing that genuinely could not be verified before the publish landed, since `mcp/`'s installed `inamprotocol` was still 0.3.1 (single-argument `acceptWork`) beforehand.
  - **Still needs the user, at their convenience**: (1) `wrangler deploy` for the Worker — no D1 migration required this time, purely a code deploy. (2) Not urgent: `npm publish` from `mcp/` for `inam-mcp` 0.2.0 (no CI workflow publishes this package's actual npm code — same "user runs `npm publish` from `mcp/`" gap noted since this package's original ship) and, after that, optionally `gh workflow run publish-mcp.yml` to update the MCP Registry's `server.json` metadata to match. (3) A decision on the deferred 7th finding, signing-string host binding — see above.
- **Item 6–7 not started:** (6) append-only transparency log (architectural, biggest lift), (7) verify whether `/jobs/search` actually 500s past 100 jobs (audit #14 didn't touch job search pagination).

### 2026-09-21 session: inam-mcp revoke tool + a Claude skill, and a new verifier-revoke gap found and closed (SPEC v0.27)

- User asked about building a Claude skill so people could try INAM, to help with the adoption bottleneck (gap 10). Before building it, researched whether the skill's own writes to the live registry could be made safer — found the existing `revoke` primitive (v0.14) was the right tool but was missing from `inam-mcp`'s tool surface. Added `inam_revoke_agent` (mcp `0.2.0` → `0.3.0`, synced the previously-stale hardcoded `McpServer` version string to match too).
- New skill (`SKILL.md` + `demo.mjs`, now living under `inam-protocol-plugin/skills/inam-protocol/` — see plugin packaging below): read-only exploration via `inam-mcp`, then an opt-in demo, then real registration. `demo.mjs` registers two throwaway identities, runs one full job→receipt cycle, revokes both. Defaults to a local dev server; a non-local `INAM_URL` alone is refused — also requires `INAM_CONFIRM=yes` — and cleanup runs in `try/finally` so a mid-run crash still revokes whatever got registered (verified by injecting a crash and confirming zero orphaned agents afterward).
- **Plugin/marketplace packaging — done.** New `.claude-plugin/plugin.json` under a new `inam-protocol-plugin/` directory (the skill's files live here now — moved from the earlier standalone `skills/inam-protocol/`, single source instead of two copies drifting apart) and a new repo-root `.claude-plugin/marketplace.json` listing it. Both validated clean with `claude plugin validate` (the marketplace one needed a `description` field added to clear a warning). Install path once pushed: `/plugin marketplace add inamprotocol/inam-protocol` then `/plugin install inam-protocol@inam-protocol-plugins`; the by-hand copy-into-`.claude/skills/` path still works too, from the new location. Schema confirmed against current Claude Code docs (via the `claude-code-guide` agent) rather than assumed from training knowledge, since this feature has been evolving.
- **Found a real, pre-existing security gap while checking whether the new tooling could cause harm**: `setVerifierStatus` already refuses to touch a revoked agent (`AGENT_REVOKED`), but `revokeAgent` never cleared `isAuthorizedVerifier` — so a verifier revoking *itself* froze that flag with no remaining path for the operator to clear it, and `hasVerifiedAttestation` (both runtimes) only ever checked `isAuthorizedVerifier`, never `revokedAt`. A self-revoked verifier's past `verified` record kept applying the 1.5x boost forever, permanently outside operator control — undermining the operator-authorization model for exactly the case (a compromised/malicious verifier) it exists to cover. Predates this session (existed since v0.14); the MCP/skill work made `revoke` meaningfully more reachable, which is what prompted checking it.
- Fixed in both runtimes: `hasVerifiedAttestation` now also requires `!agent.revokedAt`. SPEC.md → v0.27, CHANGELOG updated, server `0.7.6→0.7.7`, Worker `0.6.18→0.6.19`. New regression tests confirmed to actually catch the bug (reverted the fix, watched the new test fail, restored it, watched it pass): Node `tests/verificationFlow.test.ts` 19→20 (105→106 total), Worker `worker/tests/api.test.ts` 74→75. Both suites green, both runtimes typecheck clean.
- **Shipped and deployed, confirmed live.** Committed as two commits (`c368a83` mcp/skill, `23e1fd6` the verifier fix), pushed to `main` — CI/`Deploy site`/`Deploy docs-site` all green. `wrangler deploy` run — **this also shipped the entire v0.26 hardening batch to the Worker for the first time**, since that batch's code (committed `e652edf`, 2026-09-18) had been sitting undeployed per this file's own note ("still needs the user: `wrangler deploy`"); today's deploy carried both v0.26 and v0.27 together in one `Current Version ID: f829ad8d-1169-48b3-a12d-e27d14c6d85f`. Sanity-checked against `api.inamprotocol.org` post-deploy: `GET /v1/agents/search` 200 with real data, unknown-agent reputation 404s correctly, unsigned `POST /v1/jobs` correctly 401s `MISSING_SIGNATURE`. No D1 migration needed for either batch. No SDK/npm/PyPI publish needed (server/worker-only logic change) — `inamprotocol` on npm (0.6.0) and PyPI (0.7.0) were already exactly current, confirmed by checking both registries directly, nothing to publish there.
- **`inam-mcp` npm publish gap — closed, published `0.3.0`.** Was stuck at `0.1.0` on npm (two versions behind: the countersign-guard change and today's revoke tool). Tried the Trusted Publishing route first — user said it had been set up for this project, so added `.github/workflows/publish-npm-mcp.yml` (same OIDC + provenance pattern as `publish-npm.yml`, scoped to `mcp/`). Two dead ends found and worth remembering: (1) the workflow's read-only `test-smoke.mjs` pre-publish gate failed twice, consistently, at the exact same request — **GitHub Actions runner IPs get an HTML challenge/block page back from `api.inamprotocol.org` instead of JSON**, where the identical script against the identical URL works fine from anywhere else (this session's own shell included) — looks like Cloudflare's bot protection treating GH Actions' datacenter IP range differently, not a code issue; dropped that gate (commit `7042f3e`), kept typecheck as the pre-publish check. (2) Even after that, the actual `npm publish` step still 404'd — turned out **Trusted Publishing was never actually configured for the `inam-mcp` package specifically** (user confirmed after checking); `inamprotocol` and `inam-mcp` are unrelated packages on npm and each needs its own Trusted Publisher entry, there's no repo-level setting that covers both.
  - User published manually instead (`npm login` → confirmed `npm whoami` = `hacieskihoran`, the package's actual owner — `cd mcp && npm publish --access public`, single combined command; two earlier attempts accidentally ran from the repo root as two separate shell invocations, each publishing the wrong package — the *private* root `inam-protocol-registry` — and failing with `EPRIVATE`, harmlessly, since `private: true` blocked it). OTP/2FA prompt appeared and was completed in-browser, then the command was rerun to pick up the authorized token. Confirmed live: `registry.npmjs.org/inam-mcp` now lists both `0.1.0` and `0.3.0`, `dist-tags.latest` is `0.3.0`; downloaded the actual published tarball and grepped it for `inam_revoke_agent` to confirm the real artifact (not just package.json's version field) has the new tool.
  - **`publish-npm-mcp.yml` — Trusted Publisher now configured and confirmed working.** User added it on npmjs.com (repo `inamprotocol/inam-protocol`, workflow filename `publish-npm-mcp.yml`, environment blank — documented in `docs-design/trusted-publishing-setup.md` §1b). Re-ran the workflow afterward as a test: the failure changed from `E404`/no-permission to `"You cannot publish over the previously published versions: 0.3.0"` — i.e. auth now succeeds, it correctly just refused to republish an already-existing version. Future `inam-mcp` version bumps can publish via `gh workflow run publish-npm-mcp.yml`, no more manual `npm login`/OTP needed.
- **Still open from the prior session, unrelated to today's work**: the deferred 7th v0.26 finding (signing-string host binding, wire-breaking, needs a design decision) and item 6–7 above.
